/**
 * Backward-compat re-export — the canonical Section primitive now
 * lives in `@/components/ui/section`. Existing imports continue to
 * work until Agent 4 retrofits pages to the new path.
 */
export { Section, SectionHeader } from "@/components/ui/section";
